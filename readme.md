# levon.dev

```
# copy files to /dist/ and serve from there
npm run build

# minify css, maybe
wget --post-data="input=`cat dist/app.css`" --output-document=dist/app.css https://cssminifier.com/raw
```
